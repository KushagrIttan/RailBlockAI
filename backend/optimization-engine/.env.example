# Copy to `.env` and adjust. No secrets here — only local host/port config.
# Base URL of the .NET API. The optimizer calls {DOTNET_API_BASE}/api/optimization/data
# for tasks + corridor windows, and the .NET API calls back on port 8000.
DOTNET_API_BASE=http://localhost:5053
