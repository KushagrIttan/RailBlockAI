# Copy to `.env` and adjust. No secrets here — only local dev config.
# Target of the Vite dev-server /api proxy (see vite.config.ts).
# The frontend itself calls relative /api/... URLs, so this is the only knob.
VITE_API_TARGET=http://localhost:5053
